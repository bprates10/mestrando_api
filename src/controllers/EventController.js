// const ParseStringAsArray = require('../utils/ParseStringAsArray')
const express = require('express')
const Player = require('../models/Event')

module.exports = {

  // create event
  async create({
    req,
    res
  }) {},

  // show events
  async show({
    req,
    res
  }) {},

  // find one event
  async index({
    req,
    res
  }) {},

  // enter feedback for event
  async feedback({
    req,
    res
  }) {}
}